#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: number to be multiplied
 * @b: number to be mutiplied
 * Return: returns the result
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
