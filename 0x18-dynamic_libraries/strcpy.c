#include<stdio.h>
#include<string.h>
/**
 * _strcpy - copies a string form dest to src
 * @dest: first string to be copied to
 * @src: string to be copied from
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	return (dest);
}
