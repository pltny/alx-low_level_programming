#include<stdio.h>
#include<string.h>

/**
 * _strspn - searches for specified string
 * @s: string
 * @accept: character to searched
 * Returns: The count of character that matched
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
