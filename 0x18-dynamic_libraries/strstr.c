#include<stdio.h>
#include<string.h>

/**
 * _strstr - searches a string
 * @haystack: where string is being searched
 * @needle: the string to be searched
 *
 * Return: Always Success
 */
char *_strstr(char *haystack, char *needle)
{
	return (0);
}
